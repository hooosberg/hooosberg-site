---
import AiModelTierRanking from "./AiModelTierRanking.astro";

type Props = {
  locale?: "zh-CN" | "en";
  embedded?: boolean;
};

const { locale = "zh-CN", embedded = false } = Astro.props;
---

<AiModelTierRanking locale={locale} embedded={embedded} />
